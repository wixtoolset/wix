<element name="RemoveRegistryKey">
    <annotation>
      <documentation>
        Used for removing registry keys and all child keys during install or uninstall.
      </documentation>
      <appinfo>
        <msiRef table="Registry" href="https://learn.microsoft.com/en-us/windows/win32/msi/registry-table" />
        <msiRef table="RemoveRegistry" href="http://msdn.microsoft.com/library/aa371208.aspx" />
      </appinfo>
    </annotation>
    <complexType>
      <attribute name="Id" type="xs:string">
        <annotation>
          <documentation>
            Primary key used to identify this particular entry. If this attribute is not specified, an identifier will be
            generated by hashing the parent Component identifier, Root, Key, and Name.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Action">
        <annotation>
          <documentation>
            This is the action that will be taken for this registry value.
          </documentation>
        </annotation>
        <simpleType>
          <restriction base="xs:NMTOKEN">
            <enumeration value="removeOnInstall">
              <annotation>
                <documentation>
                  Removes a key with all its values and subkeys when the parent component is installed.
                </documentation>
              </annotation>
            </enumeration>
            <enumeration value="removeOnUninstall">
              <annotation>
                <documentation>
                  Removes a key with all its values and subkeys when the parent component is uninstalled.
                </documentation>
              </annotation>
            </enumeration>
          </restriction>
        </simpleType>
      </attribute>
      <attribute name="Key" type="xs:string">
        <annotation>
          <documentation>
            The localizable key for the registry value.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Root" type="RegistryRootType">
        <annotation>
          <documentation>
            The predefined root key for the registry value.
          </documentation>
        </annotation>
      </attribute>
      <anyAttribute namespace="##other" processContents="lax">
        <annotation>
          <documentation>
            Extensibility point in the WiX XML Schema. Schema extensions can register additional attributes at this point in the schema.
          </documentation>
        </annotation>
      </anyAttribute>
    </complexType>
  </element>