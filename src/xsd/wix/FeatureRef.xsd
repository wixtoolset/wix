<element name="FeatureRef">
    <annotation>
      <documentation>Create a reference to a Feature element in another Fragment.</documentation>
      <appinfo>
        <seeAlso ref="Feature" />
      </appinfo>
    </annotation>
    <complexType>
      <choice minOccurs="0" maxOccurs="unbounded">
        <element ref="Component" />
        <element ref="ComponentGroupRef" />
        <element ref="ComponentRef" />
        <element ref="Feature" />
        <element ref="FeatureRef" />
        <element ref="FeatureGroup" />
        <element ref="FeatureGroupRef" />
        <element ref="File" />
        <element ref="Files" />
        <element ref="MergeRef" />
        <any namespace="##other" processContents="lax">
          <annotation>
            <documentation>
              Extensibility point in the WiX XML Schema. Schema extensions can register additional
              elements at this point in the schema.
            </documentation>
          </annotation>
        </any>
      </choice>
      <attribute name="Id" type="xs:string" use="required">
        <annotation>
          <documentation>The identifier of the Feature element to reference.</documentation>
        </annotation>
      </attribute>
      <attribute name="IgnoreParent" type="YesNoTypeUnion">
        <annotation>
          <documentation>
            Normally feature references that are nested under a parent element create a connection to that
            parent. This behavior is undesirable when trying to simply reference a Feature in a different
            Fragment. Specify 'yes' to have this feature reference not create a connection to its parent.
            The default is 'no'.
          </documentation>
        </annotation>
      </attribute>
      <anyAttribute namespace="##other" processContents="lax">
        <annotation>
          <documentation>
            Extensibility point in the WiX XML Schema. Schema extensions can register additional
            attributes at this point in the schema.
          </documentation>
        </annotation>
      </anyAttribute>
    </complexType>
  </element>