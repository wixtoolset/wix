<element name="RegistryValue">
    <annotation>
      <documentation>
        Used to create a registry value. For multi-string values, this can be used to prepend or append values.

        For legacy authoring: Use several of these elements to specify each registry value in a multiString registry value. This element
        cannot be used if the Value attribute is specified unless the Type attribute is set to 'multiString'.
      </documentation>
      <appinfo>
        <msiRef table="Registry" href="https://learn.microsoft.com/en-us/windows/win32/msi/registry-table" />
        <howtoRef href="files_and_registry/write_a_registry_entry.html">How To: Write a registry entry during installation</howtoRef>
      </appinfo>
    </annotation>
    <complexType>
      <choice minOccurs="0" maxOccurs="unbounded">
        <element ref="Permission" />
        <element ref="PermissionEx">
          <annotation>
            <documentation>Can also configure the ACLs for this registry value.</documentation>
          </annotation>
        </element>
        <element ref="MultiString" />
        <element ref="MultiStringValue" />
        <any namespace="##other" processContents="lax">
          <annotation>
            <documentation>
              Extensibility point in the WiX XML Schema. Schema extensions can register additional
              elements at this point in the schema.
            </documentation>
          </annotation>
        </any>
      </choice>
      <attribute name="Id" type="xs:string">
        <annotation>
          <documentation>
            Primary key used to identify this particular entry. If this attribute is not specified, an identifier will be
            generated by hashing the parent Component identifier, Root, Key, and Name.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Root" type="RegistryRootType">
        <annotation>
          <documentation>
            The predefined root key for the registry value.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Key" type="xs:string">
        <annotation>
          <documentation>
            The localizable key for the registry value.
            If the parent element is a RegistryKey, this value may be omitted to use the
            path of the parent, or if its specified it will be appended to the path of the parent.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Name" type="xs:string">
        <annotation>
          <documentation>
            The localizable registry value name. If this attribute is not provided the default value for the registry key will
            be set instead. The Windows Installer allows several special values to be set for this attribute. You should not
            use them in WiX. Instead use appropriate values in the Action attribute to get the desired behavior.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Value" type="xs:string">
        <annotation>
          <documentation>
            Set this attribute to the localizable registry value. This value is formatted. The Windows Installer allows
            several special values to be set for this attribute. You should not use them in WiX. Instead use appropriate
            values in the Type attribute to get the desired behavior.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Type" type="RegistryValueTypeUnion">
        <annotation>
          <documentation>
            Set this attribute to the type of the desired registry key. If not specified, the default is 'string'.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="Action" type="RegistryValueActionUnion">
        <annotation>
          <documentation>
            This is the action that will be taken for this registry value. If not specified, the default is 'write'.
          </documentation>
        </annotation>
      </attribute>
      <attribute name="KeyPath" type="YesNoTypeUnion">
        <annotation>
          <documentation>
            Set this attribute to 'yes' to make this registry key the KeyPath of the parent component.
            Only one resource (registry, file, etc) can be the KeyPath of a component.
          </documentation>
        </annotation>
      </attribute>
      <anyAttribute namespace="##other" processContents="lax">
        <annotation>
          <documentation>
            Extensibility point in the WiX XML Schema. Schema extensions can register additional
            attributes at this point in the schema.
          </documentation>
        </annotation>
      </anyAttribute>
    </complexType>
  </element>