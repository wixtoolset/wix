<element name="SlipstreamMsp">
    <annotation>
      <documentation>Specifies a patch included in the same bundle that is installed when the parent MSI package is installed.</documentation>
      <appinfo>
        <seeAlso ref="MspPackage" />
        <remarks>
          <p>You can also specify that any MspPackage elements in the chain are automatically slipstreamed by setting the Slipstream attribute of an MspPackage to "yes". This will reduce the amount of authoring you need to write and will determine which msi packages can slipstream patches when building a bundle.</p>
        </remarks>
      </appinfo>
    </annotation>
    <complexType>
      <attribute name="Id" type="xs:string" use="required">
        <annotation>
          <documentation>The identifier for a MspPackage in the bundle.</documentation>
        </annotation>
      </attribute>
    </complexType>
  </element>